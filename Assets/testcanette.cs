using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class testcanette : MonoBehaviour
{
    // Start is called before the first frame update
    void Start()
    {
        Debug.Log("exists");
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
